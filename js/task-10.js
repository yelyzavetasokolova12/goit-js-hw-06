function getRandomHexColor() {
  return `#${Math.floor(Math.random() * 16777215)
    .toString(16)
    .padStart(6, 0)}`;
}

// для перевірки

// для перевірки 2

// для перевірки 3

// для перевірки 4